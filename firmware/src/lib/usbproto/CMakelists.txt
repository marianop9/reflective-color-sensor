add_library(usbproto STATIC
  usbproto.c
  usb_descriptors.c
)

target_include_directories(usbproto
  PUBLIC include 
  PRIVATE .
)

target_link_libraries(usbproto PUBLIC
  pico_stdlib
  tinyusb_device
  tinyusb_board
  ascii-comm-proto
  FreeRTOS-Kernel 
  FreeRTOS-Kernel-Heap4
)
